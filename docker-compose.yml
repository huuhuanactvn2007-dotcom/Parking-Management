services:
  #1 mysql
  mysql:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 123456
      MYSQL_DATABASE: parking_lot
    ports:
      - "127.0.0.1:3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      # Tự động thực thi schema.sql mỗi khi volume mysql khởi tạo mới:
      - ../db_backups/schema.sql:/docker-entrypoint-initdb.d/schema.sql:ro
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  #2 backend
  backend:
    build:
      context: ./parking-lot
      dockerfile: Dockerfile
    restart: always
    environment:
      DB_HOST: mysql
      DB_PORT: 3306
      DB_NAME: parking_lot
      DB_USER: root
      DB_PASSWORD: 123456
    depends_on:
      mysql:
        condition: service_healthy

  #3 frontend
  frontend:
    build:
      context: ./Parking-Lot_FE
      dockerfile: Dockerfile
    restart: always
    depends_on:
      - backend

  #4 gateway
  gateway:
    image: nginx:alpine
    restart: always
    ports:
      - "8090:8090"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./nginx/certs:/etc/nginx/certs:ro
    depends_on:
      - frontend
      - backend

volumes:
  mysql_data:
